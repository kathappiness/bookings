{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>This is the about page</h1>
                <p>This is some new text</p>
                <p>This is some new 2 text</p>
                <p>This is some new 23 text</p>
                <p>Data from the template: {{index .StringMap "test"}}</p>
                {{if ne (index .StringMap "remote_ip") ""}}
                    Your remote IP adress is {{index .StringMap "remote_ip"}}
                {{else}}
                    I don't know your IP address yet. Visit the <a href="/">Home page</a>
                {{end}}
            </div>
        </div>
    </div>
{{end}}